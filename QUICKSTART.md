# Быстрый старт

## Шаг 1: Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Проверьте установку (опционально)
python test_installation.py
```

## Шаг 2: Настройка

1. Создайте бота в Telegram:
   - Откройте [@BotFather](https://t.me/botfather)
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Скопируйте токен бота

2. (Опционально) Получите API ключ vsellm.ru:
   - Зайдите на [vsellm.ru](https://vsellm.ru)
   - Зарегистрируйтесь и получите API ключ

3. Создайте файл `.env`:
```bash
cp .env.example .env
```

4. Отредактируйте `.env` и добавьте токены:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
VSELLM_API_KEY=ваш_ключ_vsellm  # опционально
```

## Шаг 3: Запуск

```bash
python main.py
```

Вы увидите сообщение: `🤖 Бот запущен!`

## Шаг 4: Использование

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Используйте `/new` для создания превью
4. Следуйте инструкциям бота

## Возможные проблемы

### Ошибка "No module named 'telegram'"
Убедитесь, что виртуальное окружение активировано и зависимости установлены:
```bash
pip install -r requirements.txt
```

### Ошибка "Invalid token"
Проверьте правильность `TELEGRAM_BOT_TOKEN` в файле `.env`

### Шрифты не работают
Бот использует встроенные шрифты OpenCV (HERSHEY), которые работают на всех платформах без дополнительной настройки и полностью поддерживают кириллицу

### AI-генерация не работает
Это нормально, если не настроен `VSELLM_API_KEY`. Бот будет использовать градиенты вместо AI-иллюстраций.

## Примеры использования

### Минималистичное превью
1. `/new` → Выбрать "Минимализм"
2. Ввести заголовок: "Новая статья о Python"
3. Ввести описание или `/skip`
4. Получить превью!

### Превью с градиентом
1. `/new` → Выбрать "Градиент"
2. Выбрать цветовую схему (например, "Океан")
3. Ввести заголовок и описание
4. Готово!

### С собственным фоном
1. `/new` → Выбрать "Свой фон"
2. Загрузить изображение
3. Ввести заголовок и описание
4. Бот наложит текст на ваше изображение

## Следующие шаги

- Экспериментируйте с разными стилями
- Попробуйте разные градиенты
- Загрузите свои фоновые изображения
- Настройте AI-генерацию для уникальных иллюстраций
