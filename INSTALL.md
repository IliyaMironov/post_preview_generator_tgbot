# Руководство по установке

## Системные требования

- Python 3.10 или выше
- pip (обычно идет с Python)
- Интернет-соединение для установки пакетов

## Быстрая установка (3 минуты)

### 1. Установка зависимостей

```bash
# Установите все пакеты
pip install -r requirements.txt
```

Устанавливаются:
- `python-telegram-bot` - для работы с Telegram
- `opencv-python` - для генерации изображений
- `numpy` - для работы с массивами
- `requests` - для HTTP запросов
- `python-dotenv` - для переменных окружения

### 2. Проверка установки

```bash
python test_installation.py
```

Скрипт проверит:
- ✅ Все ли библиотеки установлены
- ✅ Работает ли OpenCV с текстом и кириллицей
- ✅ Настроен ли .env файл

### 3. Настройка бота

Отредактируйте файл `.env`:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

Получить токен:
1. Откройте Telegram → [@BotFather](https://t.me/botfather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

### 4. Запуск

```bash
python main.py
```

Если все правильно, вы увидите:
```
🤖 Бот запущен!
```

## Опциональная настройка AI

Для включения AI-генерации иллюстраций:

1. Зарегистрируйтесь на [vsellm.ru](https://vsellm.ru)
2. Получите API ключ
3. Добавьте в `.env`:
```env
VSELLM_API_KEY=ваш_ключ_vsellm
```

Без этого бот будет работать, но без AI-иллюстраций (будет использовать градиенты).

## Решение проблем

### Ошибка: "No module named 'cv2'"

```bash
pip install opencv-python
```

### Ошибка: "No module named 'telegram'"

```bash
pip install python-telegram-bot
```

### Ошибка: "TELEGRAM_BOT_TOKEN не установлен"

Отредактируйте файл `.env` и добавьте токен от BotFather.

### Python не найден

**Windows:**
- Скачайте с [python.org](https://python.org)
- При установке отметьте "Add Python to PATH"

**Linux:**
```bash
sudo apt install python3 python3-pip
```

**Mac:**
```bash
brew install python3
```

### pip не работает

```bash
python -m pip install -r requirements.txt
```

## Структура файлов после установки

```
imgpreviewgen/
├── main.py              ← Запускать этот файл
├── .env                 ← Настройки (токен бота)
├── requirements.txt     ← Зависимости
├── test_installation.py ← Проверка установки
├── bot/                 ← Код бота
├── generator/           ← Генераторы изображений
├── config/              ← Конфигурация
└── temp/                ← Временные файлы (создается автоматически)
```

## Дальнейшие шаги

1. ✅ Запустите бота: `python main.py`
2. ✅ Найдите бота в Telegram по имени
3. ✅ Отправьте `/start`
4. ✅ Создайте первое превью с помощью `/new`

## Обновление

Для обновления зависимостей:

```bash
pip install -r requirements.txt --upgrade
```

## Запуск в фоновом режиме (Linux/Mac)

```bash
nohup python main.py &
```

Или используйте `screen`:

```bash
screen -S bot
python main.py
# Ctrl+A, потом D для отключения
```

## Запуск как сервис (продвинутый уровень)

См. [DEPLOYMENT.md](DEPLOYMENT.md) для инструкций по развертыванию на сервере.

---

**Готово!** Теперь можно создавать превью для постов! 🎉
